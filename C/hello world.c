#include <stdio.h>   // 헤더파일  .h는 헤더파일 확장자 

int main()  	// 메인 함수 정의  
{				// 함수 범위 시작 
	
	
	printf("Hello world !");	// 표준 출력함수 
	
	return 0;		// 0을 돌려준다. = main 함수가 끝이 났다는걸 운영체제에 알려줌. 
}				// 함수 범위 끝  
